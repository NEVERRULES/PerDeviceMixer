# PerDeviceMixer project instructions

本文件是新对话和自动化开发工具接手项目时的入口。不要只依赖聊天记录；仓库中的代码和文档才是项目事实来源。

## 接手项目时先做什么

1. 阅读 `README.md`，了解用户功能、安装和当前限制。
2. 阅读 `docs/PROJECT_GUIDE.md`，了解产品边界、架构、数据流和关键设计决策。
3. 阅读 `docs/DEVELOPMENT_STATUS.md`，确认当前版本、完成度、已知问题和推荐的下一步。
4. 阅读 `CONTRIBUTING.md`，按统一方式构建、测试和发布。
5. 查看 `git status`、最近提交和当前代码；如果文档与代码冲突，以代码为准，并在本次修改中同步修正文档。

## 不可默认改变的产品边界

- 这是纯本地 Windows 桌面应用。
- 未经用户明确改变范围，不添加账号、遥测、网络 API、远程同步或后台服务器。
- 音量配置保存在当前用户的本地应用数据目录。
- 未知设备和未知应用必须采用保守策略：先学习当前状态，不擅自套用其他设备或应用的音量。
- Windows 或驱动提供、当前项目尚未直接实现的高级声音功能，应明确标注为“打开 Windows 设置入口”，不能写成应用内已实现。

## 代码边界

- `PerDeviceMixer.Core` 不直接依赖 WPF、NAudio 或 Windows UI；音频访问通过 `IAudioService` 抽象。
- `PerDeviceMixer.Audio` 负责 Core Audio、COM 生命周期、设备通知和会话事件。
- `PerDeviceMixer.App` 负责 WPF、托盘、启动项、图标和 Windows 设置入口。
- 应用设置和 Profile 结构发生变化时，考虑旧版 `profiles.json` 的兼容与迁移。
- 音频设备在蓝牙、HDMI 或休眠恢复期间可能随时消失；事件处理不得让进程崩溃。
- 应用自身恢复音量时不得被自动学习逻辑再次当作用户修改保存。

## 每次修改的完成条件

- 保留用户已有修改，不提交生成目录、个人路径、Token、日志或本机配置。
- 至少运行与修改范围相称的格式、构建和测试；默认基线见 `CONTRIBUTING.md`。
- 功能、限制、里程碑或发布流程变化时，同步更新 `README.md` 和 `docs/DEVELOPMENT_STATUS.md`。
- 新的技术决策或架构边界写入 `docs/PROJECT_GUIDE.md`，不要只留在聊天记录里。
- 每次完成修改后，重新生成当前源码的安装包并覆盖安装到本机；确认桌面快捷方式指向实际安装目录，启动后的进程也来自该目录，确保用户测试的是本次修改后的版本。
- 完成后清理本次产生且不再需要的临时、诊断和验证文件。
