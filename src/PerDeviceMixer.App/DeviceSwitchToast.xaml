<Window x:Class="PerDeviceMixer.App.DeviceSwitchToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PerDeviceMixer"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Border x:Name="Card"
            Padding="18,12"
            CornerRadius="10"
            Background="#F2191C22"
            BorderBrush="#3A414C"
            BorderThickness="1"
            IsHitTestVisible="False">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16"
                              ShadowDepth="2"
                              Opacity="0.45"
                              Color="#000000" />
        </Border.Effect>
        <StackPanel>
            <TextBlock x:Name="DeviceNameText"
                       MaxWidth="440"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="#F3F4F6"
                       TextTrimming="CharacterEllipsis" />
            <TextBlock x:Name="VolumeText"
                       Margin="0,4,0,0"
                       FontSize="12"
                       Foreground="#9AA2AF" />
            <TextBlock x:Name="BatteryText"
                       Margin="0,4,0,0"
                       FontSize="12"
                       Foreground="#78D9B0"
                       Visibility="Collapsed" />
        </StackPanel>
    </Border>
</Window>
